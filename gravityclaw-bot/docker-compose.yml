services:
  swarm:
    build: .
    container_name: gravityclaw-swarm
    restart: unless-stopped
    env_file:
      - ../.env
    environment:
      - NODE_ENV=production
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
